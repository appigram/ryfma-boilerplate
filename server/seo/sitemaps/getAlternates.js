/* const addAlternates = (result) => {
  return result.map(elem => {
  elem.links = ALTERNATES.map(lang => {
    return {
      lang: lang.locale || lang.param,
      url: `${elem.url}?lang=${lang.param}`
      };
    });
    return elem;
  });
} */
